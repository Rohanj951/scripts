#### system define variables

#*use man bash

#!/bin/bash

echo "${SHELL}  ${HOME} ${OSTYPE}"

echo "${PATH}"

echo "${$}"    #######process id 

echo "${PPID}"  ##### parent process id

echo "${PWD} ${UID} ${HOSTNAME} "

sleep 5

echo ${SECONDS}
